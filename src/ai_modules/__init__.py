"""
ai_modules — placeholder package.

Intentionally empty for now. We have NOT committed to a domain (logistics,
edge-sensing, etc.), so there is no inference code and no ML/vision
dependencies here yet. Once the team picks a feature set, the relevant
module(s) get added in this package.
"""
