# Copy to .env and fill in real values. .env is gitignored.

# Base URL of the central cloud the sync engine pushes to (none yet).
CLOUD_BASE_URL=http://localhost:9000

# How often (seconds) the sync worker checks connectivity.
SYNC_INTERVAL=15

# Where the dashboard finds the local edge API.
EDGE_API_URL=http://localhost:8000
